source_env_if_exists .envrc.local

# Octopith 기본 환경 변수 설정
export OCTOPITH_CONFIG_DIR="$(pwd)/.octopith"
export OCTOPITH_DB_PATH="$OCTOPITH_CONFIG_DIR/octopith.sqlite"

# uv를 사용한 가상환경 자동 활성화 (선택 사항)
# layout uv
