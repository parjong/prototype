[project]
name = "sample"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = []

[project.scripts]
sample = "sample:main"

[build-system]
requires = ["uv_build>=0.9.3,<0.10.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.0.1",
    "ruff>=0.14.5",
]
