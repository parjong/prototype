# 작업 계획서 (Work Plan): Octopith 개발 및 검증

본 문서는 `docs/00.DESIGN.md` 설계를 바탕으로 Octopith의 각 기능 단계를 구현하고 검증하는 상세 계획을 담고 있습니다.

## Phase 0: Environment & Project Setup
- **목표**: 보안이 보장된 실행 환경을 준비하고 사용자 설정을 완료한다.
- **주요 작업**:
    - [x] `direnv` 설정 (`.envrc`, `.envrc.local.template`).
    - [x] `.gitignore` 보안 설정.
    - [x] `uv` 기반 프로젝트 초기화 및 의존성 설정.
    - [x] **Lint/Type Check 설정**:
        - [x] `pre-commit` 도입 및 `.pre-commit-config.yaml` 작성.
        - [x] `ruff` (Linter/Formatter) 및 `pyright` (Type Checker) 통합.
    - [x] **테스트 모델 및 베드 준비**:
        - [x] `ollama.exe` 구동 확인 및 테스트 모델(`qwen2.5:3b`, `nomic-embed-text`) 확보.
        - [x] POC 저장소(`parjong/prototype`)를 테스트 대상으로 지정 및 데이터 보강.
- **검증 계획 (Validation)**:
    - [x] **환경 로드 확인**: `direnv allow` 후 `echo $OCTOPITH_GITHUB_TOKEN`이 정상 출력되는지 확인.
    - [x] **코드 품질 도구 확인**: `pre-commit run --all-files`가 정상적으로 모든 파일을 검사하는지 확인.

## Phase 1: Foundation & Ingestion (시나리오 A, B)
- **목표**: 다중 저장소 원본 데이터 수집 및 고속 키워드 검색 환경 구축.
- **주요 작업**:
    - [x] 스키마 버전 관리(`octopith.db.toml`) 및 DAO 레이어 구현.
    - [x] GitHub GraphQL 기반 비동기 수집 엔진 개발.
    - [x] CLI 커맨드 구현: `sync`, `search --fast`.
- **검증 계획 (Validation)**:
    - [x] **유닛 테스트 (Unit Tests)**:
        - [x] `pre-commit run --all-files` (ruff, pyright) 모든 검사 통과.
        - [x] `pytest`를 활용하여 DAO의 각 메서드(CRUD)가 의도대로 작동하는지 검증.
        - [x] GraphQL 쿼리 빌더가 유효한 스키마를 생성하는지 테스트.
    - [x] **통합 테스트 (Integration Tests)**:
        - [x] **Full Sync 테스트 (Target: `parjong/prototype`)**:
            - [x] `octopith sync` 실행 후 모든 이슈/댓글이 DB에 누락 없이 수집되었는지 전수 검사.
        - [x] **Partial Sync 테스트 (Target: `pallets/click`)**:
            - [x] `octopith sync --duration 5s` 실행 후 지정된 시간 내에 수집이 중단되고 정상 종료되는지 확인.
        - [x] **검색 결과 검증**: `search --fast` 결과의 정합성 및 출력 포맷 확인.
    - [x] **Phase 1 결과 보고**:
        - [x] `docs/reports/01.PHASE1_TEST_REPORT.md` 작성 및 업데이트 완료 (재현 명령어 포함).
        - [ ] 사용자 최종 승인 대기.

## Phase 2: Intelligence & Summarization (DONE ✅)
- **목표**: 로컬 LLM 연동을 통한 지식 요약 및 구조화.
- **주요 작업**:
    - [x] Inference 어댑터 및 `summaries` 관련 테이블 구축.
    - [x] **모델 관리 및 설정**: `summary-model use` 등 모델 에일리어스 설정 명령 구현.
    - [x] **가공 로직**: `TLDR`, `TROUBLESHOOTING` 요약 로직 및 `digest` 커맨드 구현.
    - [x] **검색 강화**: 요약본 정보를 함께 출력하도록 `search` 커맨드 고도화 기초 작업 완료.
- **검증 계획 (Validation)**:
    - [x] **유닛 테스트 (Unit Tests)**:
        - [x] `pre-commit run --all-files` (ruff, pyright) 모든 검사 통과.
        - [x] `TLDR` 및 `TROUBLESHOOTING` 프롬프트 템플릿 렌더링 검증.
    - [x] **통합 테스트 (Integration Tests)**:
        - **테스트 환경**: Repo: `parjong/prototype`, Model: `qwen2.5:3b`
        - **가공 시나리오**: `octopith digest --duration 30s` 실행 후 `TLDR`이 정상 생성되었는지 DB 확인 완료.
        - [x] **출력 검증**: `octopith search` 실행 시 검색 결과에 요약문이 포함되어 출력되는지 확인 완료.
    - [x] **Phase 2 결과 보고**:
        - [x] `docs/reports/02.PHASE2_TEST_REPORT.md` 작성 완료.
        - [x] 사용자 최종 승인 완료.

## Phase 3: Semantic Search & Hybrid Retrieval (DONE ✅)
 (시나리오 C, B)
- **목표**: 키워드와 의미론적 검색을 결합한 하이브리드 검색 제공.
- **주요 작업**:
    - [x] `sqlite-vec` 연동 및 `id_mapping` 기반 임베딩 파이프라인 구축.
    - [x] **모델 설정**: `embedding-model use` 명령 구현 및 `default` 에일리어스 지원.
    - [x] **통합**: `digest` 실행 시 자동 임베딩 연동 (Auto-embedding).
    - [x] RRF(Reciprocal Rank Fusion) 기반 하이브리드 검색 엔진 구현.
- **검증 계획 (Validation)**:
    - [x] **유닛 테스트 (Unit Tests)**:
        - [x] `pre-commit run --all-files` (ruff, pyright) 패스.
        - [x] RRF 랭킹 결합 로직 검증 (`search_hybrid` 메서드).
    - [x] **통합 테스트 (Integration Tests)**:
        - **테스트 환경**: Repo: `pallets/click`, Model: `nomic-embed-text`
        - [x] **시맨틱 검색**: `octopith search "rate limit"` 실행 시 `SEMANTIC_THREAD` 결과 확인 완료.
        - [x] **하이브리드 체감**: 키워드 단독(`--fast`) 대비 하이브리드 검색 결과의 풍부함 확인 완료.
    - [x] **Phase 3 결과 보고**:
        - [x] `docs/reports/03.PHASE3_TEST_REPORT.md` 작성 완료.

## Phase 4: Lifecycle & Maintenance (시나리오 D)
- **목표**: 증분 업데이트 최적화 및 지식 베이스 최신화.
- **주요 작업**:
    - [ ] 증분 가공 로직 및 계층적 무효화(Invalidation) 구현.
- **검증 계획 (Validation)**:
    - [ ] **무효화 검증 (Target: `parjong/prototype`)**: 
        - **데이터 수정**: `gh issue edit <id> --body "..."` 또는 `gh issue comment edit <id> --body "..."` 명령으로 원본 데이터 변경.
        - **증분 업데이트**: `octopith sync` 실행 후 해당 아이템의 `updated_at` 및 해시값이 갱신되었는지 확인.
        - **재가공 확인**: `octopith digest` 실행 시 수정된 아이템만 선별적으로 다시 요약되는지 `summaries.created_at` 필드를 통해 검증.
    - [ ] **Phase 4 결과 보고**:
        - [ ] `docs/reports/04.PHASE4_TEST_REPORT.md` 작성.
        - [ ] 사용자 최종 승인 대기.

---

> [!IMPORTANT]
> 각 Phase의 작업 완료 후 반드시 명시된 검증 계획을 수행하여 Pass 여부를 기록합니다.
